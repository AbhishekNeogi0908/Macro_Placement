#-----------------------------------------------------------
#TCL SCRIPT
#-----------------------------------------------------------

# 1. Setup Paths
set BASE_PATH "/mnt/c/Users/abhis/OneDrive/Desktop/Macro_Placement_WSL"
set TECH_DIR  "$BASE_PATH/tech/nangate45"
set ODB_DIR   "$BASE_PATH/adaptec1/odbFiles"
set RESULTS_DIR "$BASE_PATH/adaptec1/Results"

# 3. Load your last successful checkpoint
# This restores the design to the exact state of detailed placement
# read_db "$RESULTS_DIR/adaptec1_detailed_placed.odb"


# Automatically create the folder if it's missing
if {![file exists $RESULTS_DIR]} {
    file mkdir $RESULTS_DIR
    puts "Created results directory: $RESULTS_DIR"
}
# 2. Load Technology LEF
read_lef "$TECH_DIR/lef/NangateOpenCellLibrary.tech.lef"

# 3. Load Cell Macros (Standard) and SRAMs (fakeram)
read_lef "$TECH_DIR/lef/NangateOpenCellLibrary.macro.lef"
foreach lef_file [glob "$TECH_DIR/lef/fakeram*.lef"] {
    read_lef $lef_file
}

# Load your custom macro LEF (Generated by RosettaStone)
if {[file exists "$ODB_DIR/Nangate45.lef"]} {
    read_lef "$ODB_DIR/Nangate45.lef"
}

# 4. Load Liberty (Timing)
read_liberty "$TECH_DIR/lib/NangateOpenCellLibrary_typical.lib"

# 5. Load Design (Crucial: -continue_on_errors handles the ODB-0099 warnings)
read_def -continue_on_errors "$ODB_DIR/adaptec1_baseline.def"

# 6. Initialize the Floorplan (Required for Adaptec1 baseline)

# Try increasing the die area from 1100 to 2000 (or larger)
# Keep the core area slightly inside the die area
initialize_floorplan -site FreePDK45_38x28_10R_NP_162NW_34O \
                     -die_area {0 0 2000 2000} \
                     -core_area {50 50 1950 1950}
                     

# 7. Create rows/tracks so cells have a place to live
make_tracks
# make_rows (not needed as its already done by initialize command above)

# 8. Report status
puts "Design 'adaptec1' loaded and initialized successfully."
report_design_area


#9
# Run global placement to spread the cells
global_placement

# Save the result so you don't have to reload
write_db "$RESULTS_DIR/adaptec1_global_placed.odb"

# 10. Detailed Placement (Legalization)
# This fixes any overlapping cells from global placement
detailed_placement
write_db "$RESULTS_DIR/adaptec1_detailed_placed.odb"

# 11. Global Routing
# This generates the routing grid and estimates overflow
global_route
# no such command : report_congestion -outfile "$RESULTS_DIR/congestion_report.txt"
write_db "$RESULTS_DIR/adaptec1_global_routed.odb"

# 12. Detailed Routing (The heavy lifting) : creates the physical metal paths
#detailed_route
 

# 13. Final Evaluation
# Report wirelength and congestion
report_wire_length
report_drc

# Power Analysis
# Note: This uses default switching activity (0.1) if no VCD/SAIF file is provided
set_power_activity -activity 0.1
report_power -outfile "$RESULTS_DIR/power_report.txt"
puts "Flow complete. Check power_report.txt for details."

------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------
# 1. Setup Paths
set BASE_PATH "/mnt/c/Users/abhis/OneDrive/Desktop/Macro_Placement_WSL"
set TECH_DIR  "$BASE_PATH/tech/nangate45"
set ODB_DIR   "$BASE_PATH/adaptec1/odbFiles"
set RESULTS_DIR "$BASE_PATH/adaptec1/Results"

# 2. Load Technology LEF
read_lef "$TECH_DIR/lef/NangateOpenCellLibrary.tech.lef"
read_lef "$TECH_DIR/lef/NangateOpenCellLibrary.macro.lef"
foreach lef_file [glob "$TECH_DIR/lef/fakeram*.lef"] {
    read_lef $lef_file
}

# Load custom macro LEF (Generated by RosettaStone)
if {[file exists "$ODB_DIR/adaptec1_optimized.lef"]} {
    read_lef "$ODB_DIR/adaptec1_optimized.lef"
}

# 3. Load Liberty (Timing)
read_liberty "$TECH_DIR/lib/NangateOpenCellLibrary_typical.lib"

# 4. Load Optimized Design (Using ODB directly)
# Since you have an .odb file now, read_db is preferred over read_def
read_db "$ODB_DIR/adaptec1_optimized.odb"

# --- REMOVED initialize_floorplan and make_tracks ---
# These are already baked into your optimized ODB file. 
# Running them again will overwrite your optimized coordinates.

# 5. LOCK your optimized cells in place
# This is the vital step to respect your Python optimization
set_db [get_cells *] .placement_status FIXED

# 6. Legalize (Clean up minor overlaps from Python conversion)
# This keeps your main optimization structure while fixing design rule violations
detailed_placement

# 7. Global Routing
global_route

# 8. Save result
write_db "$RESULTS_DIR/adaptec1_optimized_routed.odb"